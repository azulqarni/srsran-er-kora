# Imports: google.protobuf, zmq, and numpy (send_mcs.py/send_weight.py).
# Protobuf 6.33 supports the distro protoc 3.21 generator used here.
protobuf==6.33.6
pyzmq==27.1.0

# Keep NumPy 1.23 for the legacy muApps, while selecting compatible base wheels
# for the default Python versions on Ubuntu 24.04 and Debian 13.
numpy==1.23.5; python_version < "3.12"
numpy==1.26.4; python_version == "3.12"
numpy==2.2.6; python_version == "3.13"
