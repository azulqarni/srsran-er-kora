diff --git a/CMakeLists.txt b/CMakeLists.txt
index f1bc932737..a8f28e2b74 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -460,6 +460,9 @@ else (ENABLE_ZEROMQ)
     unset(ZEROMQ_FOUND CACHE)
 endif (ENABLE_ZEROMQ)
 
+# Protocol Buffers
+find_package(Protobuf REQUIRED)
+
 if (ENABLE_LIBNUMA)
     find_package(NUMA)
     if (NUMA_FOUND)
diff --git a/lib/CMakeLists.txt b/lib/CMakeLists.txt
index 9528ada3e9..ef7aad2761 100644
--- a/lib/CMakeLists.txt
+++ b/lib/CMakeLists.txt
@@ -57,3 +57,4 @@ add_subdirectory(security)
 add_subdirectory(srslog)
 add_subdirectory(srsvec)
 add_subdirectory(support)
+add_subdirectory(edgeric)
diff --git a/lib/radio/zmq/CMakeLists.txt b/lib/radio/zmq/CMakeLists.txt
index ba9b343eaa..37717f618d 100644
--- a/lib/radio/zmq/CMakeLists.txt
+++ b/lib/radio/zmq/CMakeLists.txt
@@ -28,6 +28,6 @@ set(SOURCES_ZMQ
         radio_zmq_tx_stream.cpp)
 add_library(srsran_radio_zmq STATIC ${SOURCES_ZMQ})
 
-target_link_libraries(srsran_radio_zmq ${ZEROMQ_LIBRARIES} fmt srslog)
+target_link_libraries(srsran_radio_zmq ${ZEROMQ_LIBRARIES} fmt srslog edgeric)
 
 add_to_exported_libs(srsran_radio_zmq)
diff --git a/lib/scheduler/CMakeLists.txt b/lib/scheduler/CMakeLists.txt
index 8f1e97876b..0f8c0703a8 100644
--- a/lib/scheduler/CMakeLists.txt
+++ b/lib/scheduler/CMakeLists.txt
@@ -52,4 +52,4 @@ add_library(srsran_sched STATIC ${SOURCES}
         $<TARGET_OBJECTS:sched_cell>
         $<TARGET_OBJECTS:common_sched>
         $<TARGET_OBJECTS:ue_sched>)
-target_link_libraries(srsran_sched srsran_ran sched_config sched_ue_context sched_support scheduler_logger)
+target_link_libraries(srsran_sched srsran_ran sched_config sched_ue_context sched_support scheduler_logger edgeric)
diff --git a/lib/scheduler/cell_scheduler.cpp b/lib/scheduler/cell_scheduler.cpp
index 4b8f183f70..8f3c09c03e 100644
--- a/lib/scheduler/cell_scheduler.cpp
+++ b/lib/scheduler/cell_scheduler.cpp
@@ -23,9 +23,14 @@
 #include "cell_scheduler.h"
 #include "logging/scheduler_metrics_handler.h"
 #include "ue_scheduling/ue_scheduler_impl.h"
+#include "../edgeric/edgeric.h"
 
 using namespace srsran;
 
+namespace {
+uint32_t tti_counter = 0;
+}
+
 cell_scheduler::cell_scheduler(const scheduler_expert_config&                  sched_cfg,
                                const sched_cell_configuration_request_message& msg,
                                const cell_configuration&                       cell_cfg_,
@@ -90,6 +95,10 @@ void cell_scheduler::run_slot(slot_point sl_tx)
 {
   // Mark the start of the slot.
   auto slot_start_tp = std::chrono::high_resolution_clock::now();
+  ++tti_counter;
+  edgeric::setTTI(tti_counter);
+  edgeric::get_weights_from_er();
+  edgeric::get_mcs_from_er();
 
   // If there are skipped slots, handle them. Otherwise, the cell grid and cached results are not correctly cleared.
   if (SRSRAN_LIKELY(res_grid.slot_tx().valid())) {
@@ -134,6 +143,8 @@ void cell_scheduler::run_slot(slot_point sl_tx)
 
   // > Schedule UE DL and UL data.
   ue_sched.run_slot(sl_tx);
+  edgeric::printmyvariables();
+  edgeric::send_to_er();
 
   // > Mark stop of the slot processing
   auto slot_stop_tp = std::chrono::high_resolution_clock::now();
diff --git a/lib/scheduler/ue_scheduling/grant_params_selector.cpp b/lib/scheduler/ue_scheduling/grant_params_selector.cpp
index 67bd48d906..5634ca2f84 100644
--- a/lib/scheduler/ue_scheduling/grant_params_selector.cpp
+++ b/lib/scheduler/ue_scheduling/grant_params_selector.cpp
@@ -27,7 +27,9 @@
 #include "../support/prbs_calculator.h"
 #include "../support/rb_helper.h"
 #include "../ue_context/ue_cell.h"
+#include "../../edgeric/edgeric.h"
 #include "srsran/ran/transform_precoding/transform_precoding_helpers.h"
+#include <cmath>
 
 using namespace srsran;
 using namespace sched_helper;
@@ -284,6 +286,20 @@ static std::optional<dl_sched_context> get_dl_sched_context(const slice_ue&
       }
       mcs     = mcs_prbs_sel.value().mcs;
       nof_rbs = mcs_prbs_sel.value().nof_prbs;
+
+      const uint16_t rnti = static_cast<uint16_t>(u.crnti());
+      if (auto mcs_override = edgeric::get_mcs(rnti)) {
+        mcs = sch_mcs_index{mcs_override.value()};
+      }
+      if (auto weight = edgeric::get_weights(rnti)) {
+        float clamped = std::max(0.0f, std::min(1.0f, weight.value()));
+        unsigned weighted_rbs =
+            static_cast<unsigned>(std::round(clamped * static_cast<float>(nof_rb_lims.stop())));
+        weighted_rbs = std::min(nof_rb_lims.stop(), std::max(nof_rb_lims.start(), weighted_rbs));
+        if (weighted_rbs != 0) {
+          nof_rbs = weighted_rbs;
+        }
+      }
     } else {
       // ReTx Case.
       nof_layers = h_dl->get_grant_params().nof_layers;
diff --git a/lib/scheduler/ue_scheduling/intra_slice_scheduler.cpp b/lib/scheduler/ue_scheduling/intra_slice_scheduler.cpp
index 913549d7ab..c4b4c2c0b2 100644
--- a/lib/scheduler/ue_scheduling/intra_slice_scheduler.cpp
+++ b/lib/scheduler/ue_scheduling/intra_slice_scheduler.cpp
@@ -22,6 +22,7 @@
 
 #include "intra_slice_scheduler.h"
 #include "srsran/ran/pdcch/search_space.h"
+#include "../../edgeric/edgeric.h"
 
 using namespace srsran;
 
@@ -149,10 +150,24 @@ void intra_slice_scheduler::post_process_results()
 void intra_slice_scheduler::dl_sched(dl_ran_slice_candidate slice, scheduler_policy& policy)
 {
   srsran_sanity_check(slice.remaining_rbs() > 0, "Invalid slice slice");
-  if (slice.get_slice_ues().empty()) {
+  const slice_ue_repository& slice_ues = slice.get_slice_ues();
+  if (slice_ues.empty()) {
     return;
   }
 
+  for (const slice_ue& u : slice_ues) {
+    const ue_cell& ue_cc = u.get_cc();
+    const uint16_t rnti  = static_cast<uint16_t>(ue_cc.rnti());
+    const float    cqi   = ue_cc.link_adaptation_controller().get_effective_cqi();
+    const float    snr   = ue_cc.channel_state_manager().get_pusch_snr();
+    const uint32_t dl_buffer = u.pending_dl_newtx_bytes();
+    const uint32_t ul_buffer = u.pending_ul_newtx_bytes();
+    edgeric::set_cqi(rnti, cqi);
+    edgeric::set_snr(rnti, snr);
+    edgeric::set_dl_buffer(rnti, dl_buffer);
+    edgeric::set_ul_buffer(rnti, ul_buffer);
+  }
+
   if (slice.get_slot_tx() != pdsch_slot) {
     pdsch_slot = slice.get_slot_tx();
     update_used_dl_vrbs(slice);
diff --git a/lib/scheduler/ue_scheduling/ue_cell_grid_allocator.cpp b/lib/scheduler/ue_scheduling/ue_cell_grid_allocator.cpp
index a0befd714e..5295b88882 100644
--- a/lib/scheduler/ue_scheduling/ue_cell_grid_allocator.cpp
+++ b/lib/scheduler/ue_scheduling/ue_cell_grid_allocator.cpp
@@ -26,6 +26,7 @@
 #include "grant_params_selector.h"
 #include "srsran/scheduler/result/dci_info.h"
 #include "srsran/support/error_handling.h"
+#include "../../edgeric/edgeric.h"
 
 using namespace srsran;
 
@@ -274,6 +275,7 @@ void ue_cell_grid_allocator::set_pdsch_params(dl_grant_info&
     }
     mcs_tbs_info = mcs_or_error.value_or(sch_mcs_tbs{sch_mcs_index{0}, 0});
   }
+  edgeric::set_dl_tbs(static_cast<uint16_t>(ue_cc.rnti()), mcs_tbs_info.tbs);
 
   // Mark resources as occupied in the ResourceGrid.
   pdsch_alloc.dl_res_grid.fill(grant_info{scs, pdsch_td_cfg.symbols, crbs.first});
diff --git a/lib/scheduler/ue_scheduling/ue_event_manager.cpp b/lib/scheduler/ue_scheduling/ue_event_manager.cpp
index dfa6f704c6..a653d04169 100644
--- a/lib/scheduler/ue_scheduling/ue_event_manager.cpp
+++ b/lib/scheduler/ue_scheduling/ue_event_manager.cpp
@@ -27,6 +27,7 @@
 #include "../support/sr_helper.h"
 #include "../uci_scheduling/uci_scheduler_impl.h"
 #include "srsran/support/memory_pool/unbounded_object_pool.h"
+#include "../../edgeric/edgeric.h"
 
 using namespace srsran;
 
@@ -504,6 +505,7 @@ void ue_event_manager::handle_crc_indication(const ul_crc_indication& crc_ind)
                                                                                               crc_ptr->harq_id,
                                                                                               crc_ptr->tb_crc_success,
                                                                                               crc_ptr->ul_sinr_dB});
+              edgeric::set_rx_bytes(static_cast<uint16_t>(crc_ptr->rnti), units::bytes{(unsigned)tbs}.value());
 
               // Notify metrics handler.
               du_cells[ue_cc.cell_index].metrics->handle_crc_indication(sl_rx, *crc_ptr, units::bytes{(unsigned)tbs});
@@ -538,6 +540,7 @@ void ue_event_manager::handle_harq_ind(ue_cell&                               ue
           result->update == dl_harq_process_handle::status_update::nacked) {
         du_cells[ue_cc.cell_index].metrics->handle_dl_harq_ack(
             ue_cc.ue_index, result->update == dl_harq_process_handle::status_update::acked, tbs);
+        edgeric::set_tx_bytes(static_cast<uint16_t>(ue_cc.rnti()), tbs.value());
       }
     }
   }
